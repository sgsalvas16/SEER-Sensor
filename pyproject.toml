[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
extend-select = ["W", "E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
# Embedded HTML/CSS/JS dashboard cannot be line-wrapped
"Automation/SEER/scout_receiver/server.py" = ["E501"]
